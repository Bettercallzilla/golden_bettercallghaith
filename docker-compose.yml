version: '3.8'
services:
  instagram-dm-api:
    build: .
    image: instagram-dm-api:latest
    restart: always
    ports:
      - "3013:3013"
    volumes:
      - sessions_data:/data/sessions
volumes:
  sessions_data:
    driver: local
